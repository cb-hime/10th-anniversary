HOTFIX

この修正版では、初回起動時の白い画面をなくすため、
STARTを押した後は白画面を消さず、黒いローディング画面へフェードしてから
MindARを開始するように変更します。

変更内容（script.js）
1. START画面をすぐ消さない
2. 黒いLoading画面を先に表示
3. カメラ開始後、0.5秒待ってからSTART画面を閉じる
4. スキャンUI表示

style.css
Loading画面を黒背景固定に変更。
